# Price Dumping Feature (Автодемпинг цен)

## Описание

Система автоматического мониторинга цен конкурентов и установки оптимальной цены на товары.

## Как это работает

1. **Мониторинг каждые 5 минут** - Worker проверяет цены конкурентов для всех товаров с включенным автодемпингом
2. **Находит минимальную цену** - Определяет самую низкую цену среди всех конкурентов на Kaspi
3. **Устанавливает на 1₸ дешевле** - Автоматически обновляет вашу цену на 1 тенге ниже конкурентов
4. **Защита от убытков** - Если цена конкурентов падает ниже вашего минимального порога, система не обновляет цену

## Настройка через Telegram Bot

### Глобальное включение/выключение

```
⚙️ Settings → Toggle Auto-Dumping
```

Включает или отключает автодемпинг для всех ваших товаров.

### Управление товарами

#### Просмотр товаров с автодемпингом

```
💰 Price Dumping → View Products
```

Показывает список товаров:
- ✅ С включенным автодемпингом
- ⚠️ Товары, достигшие минимальной цены
- Текущие и минимальные цены
- Цены конкурентов

#### Включить автодемпинг для товара

```
💰 Price Dumping → Enable for Product
```

Формат: `SKU МИНИМАЛЬНАЯ_ЦЕНА`

Пример:
```
ABC123 15000
```

Где:
- `ABC123` - SKU (артикул) товара
- `15000` - минимальная цена в тенге (ниже этой цены система не опустит цену)

#### Выключить автодемпинг для товара

```
💰 Price Dumping → Disable for Product
```

Формат: `SKU`

Пример:
```
ABC123
```

## Структура данных

### Product Model

```go
type Product struct {
    // ... другие поля

    MinPrice           float64   // Минимальная цена (порог)
    CompetitorMinPrice float64   // Мин. цена конкурентов
    AutoDumpingEnabled bool      // Включен ли автодемпинг
    LastPriceCheckAt   time.Time // Последняя проверка цен
}
```

### User Model

```go
type User struct {
    // ... другие поля

    AutoDumpingEnabled bool // Глобальный переключатель
}
```

## API Endpoints (Mock)

### Kaspi Client

```go
// Получить цены конкурентов
func (c *Client) GetCompetitorPrices(productExternalID string) ([]CompetitorPrice, error)

// Обновить цену товара
func (c *Client) UpdateProductPrice(productExternalID string, newPrice float64) error
```

**Примечание:** Текущая реализация использует mock данные. Для работы с реальным API Kaspi нужно заменить реализацию в `internal/marketplace/kaspi/price.go`.

## Worker Schedule

```go
// Каждые 5 минут
"*/5 * * * *" → PriceDumpingService.ProcessAllUsers()
```

### Логика обработки

1. Получить все активные Kaspi ключи
2. Для каждого пользователя:
   - Получить товары с `auto_dumping_enabled = true`
   - Для каждого товара:
     - Запросить цены конкурентов через Kaspi API
     - Найти минимальную цену
     - Рассчитать новую цену = min_competitor_price - 1
     - Проверить против `min_price`
     - Если новая цена >= `min_price`: обновить через Kaspi API
     - Если новая цена < `min_price`: пропустить, логировать

## Примеры использования

### Включение автодемпинга для товара

```
Пользователь: ABC123 15000
Бот: ✅ Автодемпинг включен!

Товар: Смартфон Samsung Galaxy A54
SKU: ABC123
Минимальная цена: 15000₸

Система начнет отслеживать цены конкурентов каждые 5 минут.
```

### Сценарий работы

**Начальное состояние:**
- Ваша цена: 20,000₸
- Мин. порог: 15,000₸
- Цены конкурентов: 19,500₸, 21,000₸, 18,000₸

**После 1-й проверки (через 5 минут):**
- Мин. цена конкурентов: 18,000₸
- Новая ваша цена: **17,999₸** (18,000 - 1)
- Обновлено ✅

**После 2-й проверки (через 10 минут):**
- Мин. цена конкурентов: 16,000₸
- Новая ваша цена: **15,999₸** (16,000 - 1)
- Обновлено ✅

**После 3-й проверки (через 15 минут):**
- Мин. цена конкурентов: 14,500₸
- Новая цена: 14,499₸ (14,500 - 1)
- **НЕ обновлено** ⚠️ (ниже минимального порога 15,000₸)
- Ваша цена остается: 15,999₸

## Мониторинг и логи

### Логирование

```go
logger.Log.Info("Price updated successfully",
    zap.String("product_id", product.ID),
    zap.String("product_name", product.Name),
    zap.Float64("old_price", product.Price),
    zap.Float64("new_price", newPrice),
    zap.Float64("min_competitor_price", minCompetitorPrice),
    zap.Float64("min_threshold", product.MinPrice),
)
```

### Метрики Worker

```
Starting price dumping cycle
Processing products for dumping: user_id=123, products_count=5
Price updated successfully: product_id=abc, old_price=20000, new_price=17999
Price below minimum threshold, skipping: product_id=def, new_price=14499, min_price=15000
User products processed: user_id=123, processed=5, updated=3
Price dumping cycle completed: success=10, errors=0
```

## Безопасность

1. **Минимальный порог** - Защита от демпинга ниже себестоимости
2. **Шифрование API ключей** - Все Kaspi API ключи зашифрованы AES-256-GCM
3. **Per-product контроль** - Каждый товар можно включить/выключить отдельно
4. **Глобальный выключатель** - Быстрое отключение всей системы через Settings

## Ограничения

1. **Частота обновлений**: Каждые 5 минут (настраивается в cron)
2. **Mock данные**: Текущая версия использует случайные цены конкурентов
3. **Kaspi API**: Требуется реальная интеграция с Kaspi API
4. **Rate limiting**: При реальной интеграции нужно учесть лимиты Kaspi API

## Roadmap

- [ ] Интеграция с реальным Kaspi API
- [ ] Настраиваемый margin (не только -1₸, но и -5₸, -10₸, -1%)
- [ ] История изменения цен
- [ ] Уведомления в Telegram при достижении минимальной цены
- [ ] Графики мониторинга цен
- [ ] Bulk операции (включить/выключить для всех товаров категории)
- [ ] Расписание автодемпинга (например, только в рабочие часы)
- [ ] A/B тестирование стратегий ценообразования

## Troubleshooting

### Товар не обновляется

1. Проверьте, включен ли глобальный автодемпинг: `⚙️ Settings`
2. Проверьте, включен ли автодемпинг для товара: `💰 Price Dumping → View Products`
3. Проверьте логи worker: `docker-compose logs worker`
4. Убедитесь, что товар в наличии (`current_stock > 0`)

### Цена обновляется слишком редко

Worker запускается каждые 5 минут. Если нужно чаще - измените cron выражение в `cmd/worker/main.go`:

```go
// Было: "*/5 * * * *" (каждые 5 минут)
// Стало: "*/2 * * * *" (каждые 2 минуты)
```

### Система не находит конкурентов

Mock реализация всегда возвращает случайные цены конкурентов. В production нужно:

1. Реализовать настоящий API запрос в `internal/marketplace/kaspi/price.go`
2. Обработать случай, когда конкурентов нет
3. Добавить кеширование для снижения нагрузки на API
